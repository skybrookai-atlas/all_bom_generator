[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA routing — serve index.html for all routes so React Router handles them
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
