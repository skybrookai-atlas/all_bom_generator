// Re-export inferred types from schema for convenience
export type {
  GateConfig,
} from '../schemas/gate.schema';

export type {
  GateType,
  GatePostSize,
  HingeType,
  LatchType,
} from '../schemas/gate.schema';
